--- a/src/pages/MyQueue.tsx
+++ b/src/pages/MyQueue.tsx
@@ -260,87 +260,42 @@
 
                     {/* Toolbar - Status Filter, Counter, Sort */}
                     <div className="flex items-center justify-between bg-surface-highlight/30 px-6 py-3 rounded-xl border border-white/5">
-                        {/* Status Filter - Custom Dropdown */}
+                        {/* Status Filter */}
                         <div className="flex items-center gap-3">
                             <span className="text-xs font-bold text-text-muted uppercase tracking-wider">Exibir:</span>
-                            <div className="relative" ref={statusFilterRef}>
-                                <button
-                                    onClick={() => setShowStatusFilterDropdown(!showStatusFilterDropdown)}
-                                    className={`bg-surface-dark border text-white text-sm font-medium rounded-lg px-4 py-2 cursor-pointer transition-all focus:outline-none flex items-center gap-2 min-w-[220px] justify-between ${showStatusFilterDropdown ? 'border-primary ring-2 ring-primary/50 shadow-[0_0_20px_rgba(19,236,91,0.3)]' : 'border-white/10 hover:border-primary/50'}`}
-                                >
-                                    <span>{statusFilter === 'OPEN' ? 'Abertas' : (statusFilter === 'OPEN_MY_PART_DONE' ? 'Abertas (minha parte entregue)' : 'Entregues')}</span>
-                                    <span className={`material-symbols-outlined text-sm transition-all ${showStatusFilterDropdown ? 'rotate-180 text-primary' : 'text-text-muted'}`}>expand_more</span>
-                                </button>
-                                
-                                {showStatusFilterDropdown && (
-                                    <div className="absolute top-full left-0 mt-2 w-full bg-surface-dark border border-white/10 rounded-xl shadow-2xl z-[100] overflow-hidden animate-scale-in">
-                                        <div className="p-1">
-                                            {[
-                                                { value: 'OPEN', label: 'Abertas' },
-                                                { value: 'OPEN_MY_PART_DONE', label: 'Abertas (minha parte entregue)' },
-                                                { value: 'DELIVERED', label: 'Entregues' }
-                                            ].map((option) => (
-                                                <button
-                                                    key={option.value}
-                                                    onClick={() => {
-                                                        setStatusFilter(option.value as any);
-                                                        setShowStatusFilterDropdown(false);
-                                                    }}
-                                                    className="w-full text-left px-3 py-2.5 text-sm text-white hover:bg-white/5 rounded-lg transition-colors flex items-center justify-between"
-                                                >
-                                                    <span>{option.label}</span>
-                                                    {statusFilter === option.value && <span className="material-symbols-outlined text-primary text-[16px]">check</span>}
-                                                </button>
-                                            ))}
-                                        </div>
-                                    </div>
-                                )}
-                            </div>
+                            <Dropdown
+                                value={statusFilter}
+                                onChange={(val) => setStatusFilter(val as any)}
+                                options={[
+                                    { value: 'OPEN', label: 'Abertas' },
+                                    { value: 'OPEN_MY_PART_DONE', label: 'Abertas (minha parte entregue)' },
+                                    { value: 'DELIVERED', label: 'Entregues' }
+                                ]}
+                                minWidth="220px"
+                            />
                         </div>
 
                         {/* Task Counter */}
                         <div className="flex items-center gap-2 text-text-secondary">
                             <span className="material-symbols-outlined text-lg">task_alt</span>
                             <span className="text-sm font-medium">{displayTasks.length} {displayTasks.length === 1 ? 'tarefa' : 'tarefas'}</span>
                         </div>
 
-                        {/* Sort Dropdown - Custom */}
+                        {/* Sort Dropdown */}
                         <div className="flex items-center gap-3">
                             <span className="text-xs font-bold text-text-muted uppercase tracking-wider">Ordenar:</span>
-                            <div className="relative" ref={sortDropdownRef}>
-                                <button
-                                    onClick={() => setShowSortDropdown(!showSortDropdown)}
-                                    className={`bg-surface-dark border text-white text-sm font-medium rounded-lg px-4 py-2 cursor-pointer transition-all focus:outline-none flex items-center gap-2 min-w-[200px] justify-between ${showSortDropdown ? 'border-primary ring-2 ring-primary/50 shadow-[0_0_20px_rgba(19,236,91,0.3)]' : 'border-white/10 hover:border-primary/50'}`}
-                                >
-                                    <span>
-                                        {sortOrder === 'PRIORITY' ? 'Por prioridade' :
-                                            sortOrder === 'TITLE' ? 'Por título' :
-                                                sortOrder === 'CREATED_AT' ? 'Por data de criação' :
-                                                    sortOrder === 'URGENCY' ? 'Por urgência' :
-                                                        'Manual (Drag & Drop)'}
-                                    </span>
-                                    <span className={`material-symbols-outlined text-sm transition-all ${showSortDropdown ? 'rotate-180 text-primary' : 'text-text-muted'}`}>expand_more</span>
-                                </button>
-                                
-                                {showSortDropdown && (
-                                    <div className="absolute top-full right-0 mt-2 w-full bg-surface-dark border border-white/10 rounded-xl shadow-2xl z-[100] overflow-hidden animate-scale-in">
-                                        <div className="p-1">
-                                            {[
-                                                { value: 'PRIORITY', label: 'Por prioridade' },
-                                                { value: 'TITLE', label: 'Por título' },
-                                                { value: 'CREATED_AT', label: 'Por data de criação' },
-                                                { value: 'URGENCY', label: 'Por urgência' },
-                                                { value: 'MANUAL', label: 'Manual (Drag & Drop)' }
-                                            ].map((option) => (
-                                                <button
-                                                    key={option.value}
-                                                    onClick={() => {
-                                                        setSortOrder(option.value as any);
-                                                        setShowSortDropdown(false);
-                                                    }}
-                                                    className="w-full text-left px-3 py-2.5 text-sm text-white hover:bg-white/5 rounded-lg transition-colors flex items-center justify-between"
-                                                >
-                                                    <span>{option.label}</span>
-                                                    {sortOrder === option.value && <span className="material-symbols-outlined text-primary text-[16px]">check</span>}
-                                                </button>
-                                            ))}
-                                        </div>
-                                    </div>
-                                )}
-                            </div>
+                            <Dropdown
+                                value={sortOrder}
+                                onChange={(val) => setSortOrder(val as any)}
+                                options={[
+                                    { value: 'PRIORITY', label: 'Por prioridade' },
+                                    { value: 'TITLE', label: 'Por título' },
+                                    { value: 'CREATED_AT', label: 'Por data de criação' },
+                                    { value: 'URGENCY', label: 'Por urgência' },
+                                    { value: 'MANUAL', label: 'Manual (Drag & Drop)' }
+                                ]}
+                                minWidth="200px"
+                                align="right"
+                            />
                         </div>
                     </div>
